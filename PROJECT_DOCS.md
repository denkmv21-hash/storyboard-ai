# 🎬 Storyboard AI — Полная документация проекта

**Версия**: 1.0  
**Дата**: 20 февраля 2026  
**Статус**: ✅ Работает

---

# 📖 ЧАСТЬ 1: ОБЩЕЕ ОПИСАНИЕ ПРОЕКТА

## 1.1 Что такое Storyboard AI

**Storyboard AI** — это SaaS-сервис для автоматической генерации раскадровок из сценариев с помощью искусственного интеллекта.

### Основное назначение

Сервис превращает текстовые сценарии (форматы FDX, PDF, TXT) в профессиональные визуальные раскадровки для:
- 🎥 Кинематографистов
- 📺 Режиссёров
- 🎮 Разработчиков игр
- 📱 Создателей контента
- 🎓 Студентов киношкол

### Ключевые возможности

| Возможность | Описание |
|-------------|----------|
| 📄 **Загрузка сценариев** | Поддержка форматов FDX, PDF, TXT → парсинг в JSON-сцены |
| 🤖 **AI-генерация изображений** | Генерация через SDXL (Replicate API) |
| 🎭 **Сохранение персонажей** | Character Consistency через IP-Adapter |
| 🎬 **Редактирование** | Настройка ракурсов, стилей, композиции |
| 📤 **Экспорт** | PDF, PNG, MP4 форматы |
| 💳 **Подписки** | Stripe + кредитная система |

---

## 1.2 Целевая аудитория

### Основные сегменты

1. **Независимые кинематографисты** (40%)
   - Бюджет: $10-50/мес
   - Потребность: быстрая визуализация идей

2. **Студии и продакшены** (30%)
   - Бюджет: $100-500/мес
   - Потребность: командная работа, экспорт

3. **Студенты и обучающиеся** (20%)
   - Бюджет: $0-10/мес
   - Потребность: учебные проекты

4. **Блогеры и контент-мейкеры** (10%)
   - Бюджет: $10-30/мес
   - Потребность: планирование видео

---

## 1.3 Бизнес-модель

### Тарифные планы

| План | Цена | Кредиты | Проекты | Сцены | Функции |
|------|------|---------|---------|-------|---------|
| **Free** | $0 | 10/мес | 3 | 30 | Базовое качество, водяной знак |
| **Basic** | $9.99 | 100/мес | 10 | 100 | HD качество, без водяного знака |
| **Pro** | $29.99 | 500/мес | ∞ | 500 | 4K качество, приоритетная генерация |
| **Enterprise** | $99.99 | 2000/мес | ∞ | ∞ | API доступ, команда, кастомные модели |

### Источники дохода

1. **Подписки** (70% дохода)
2. **Покупка кредитов** (20%)
3. **Enterprise лицензии** (10%)

### Прогноз доходов

| Месяц | Пользователи | MRR |
|-------|-------------|-----|
| 1 | 50 | $500 |
| 3 | 200 | $2,000 |
| 6 | 500 | $6,000 |
| 12 | 2,000 | $25,000 |

---

## 1.4 Конкурентные преимущества

### Прямые конкуренты

| Конкурент | Цена | Недостатки | Наше преимущество |
|-----------|------|------------|-----------------|
| Plot | $15-50/мес | Нет character consistency | Сохранение персонажей |
| Storyboarder | Бесплатно | Ручная рисовка | AI автоматизация |
| Boords | $39/мес | Нет AI генерации | Полная автоматизация |

### Уникальное торговое предложение (УТП)

> **"Преврати сценарий в раскадровку за 5 минут с сохранением персонажей"**

1. ⚡ **Скорость**: 5 минут vs 2-3 часа вручную
2. 🎭 **Character Consistency**: персонажи узнаваемы в кадрах
3. 💰 **Цена**: от $0 vs $39-100/мес у конкурентов
4. 🌐 **Доступность**: веб-интерфейс, нет установки

---

## 1.5 Пользовательские сценарии

### Сценарий 1: Регистрация и первый проект

```
1. Пользователь заходит на сайт
2. Нажимает "Попробовать бесплатно"
3. Регистрируется (email + пароль)
4. Получает 10 бесплатных кредитов
5. Создаёт первый проект
6. Загружает сценарий (PDF/TXT)
7. Система парсит сценарий на сцены
8. Пользователь генерирует изображения
9. Экспортирует результат в PDF
```

### Сценарий 2: Генерация раскадровки

```
1. Пользователь выбирает проект
2. Загружает сценарий (формат FDX/PDF/TXT)
3. Система анализирует сценарий
4. Выделяет сцены и описания
5. Пользователь настраивает стиль
6. Нажимает "Генерировать"
7. AI создаёт изображения (30-60 сек)
8. Пользователь просматривает результат
9. Редактирует при необходимости
10. Экспортирует в нужном формате
```

### Сценарий 3: Покупка подписки

```
1. Пользователю не хватает кредитов
2. Переходит в раздел "Тарифы"
3. Выбирает план (Basic/Pro/Enterprise)
4. Нажимает "Оплатить"
5. Перенаправляется на Stripe Checkout
6. Вводит данные карты
7. Оплата подтверждается
8. Кредиты начисляются мгновенно
9. Получает email-подтверждение
```

---

## 1.6 Дорожная карта развития

### Q1 2026 (Январь-Март)
- [x] MVP с базовой генерацией
- [x] Регистрация и аутентификация
- [x] Загрузка сценариев
- [ ] Интеграция платежей
- [ ] Мобильная адаптация

### Q2 2026 (Апрель-Июнь)
- [ ] Character Consistency 2.0
- [ ] Экспорт в MP4 (анимация)
- [ ] Командная работа
- [ ] API для разработчиков
- [ ] Интеграция с Final Draft

### Q3 2026 (Июль-Сентябрь)
- [ ] Мобильное приложение (iOS/Android)
- [ ] Offline режим
- [ ] Библиотека ассетов
- [ ] Шаблоны стилей
- [ ] Голосовые команды

### Q4 2026 (Октябрь-Декабрь)
- [ ] Real-time коллаборация
- [ ] VR/AR просмотр
- [ ] Интеграция с Adobe Premiere
- [ ] Локализация (5 языков)
- [ ] White-label решения

---

# ⚙️ ЧАСТЬ 2: ТЕХНИЧЕСКАЯ ДОКУМЕНТАЦИЯ

## 2.1 Архитектура системы

### Общая схема

```
┌─────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                        │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              Next.js 14 Frontend                    │   │
│  │  - React Components                                 │   │
│  │  - Zustand State Management                         │   │
│  │  - Tailwind CSS                                     │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ HTTPS
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                         API LAYER                           │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              Express.js API Server                  │   │
│  │  - REST Endpoints                                   │   │
│  │  - Authentication (JWT)                             │   │
│  │  - Validation (Zod)                                 │   │
│  │  - Rate Limiting                                    │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            │
                            │
            ┌───────────────┼───────────────┐
            │               │               │
            ▼               ▼               ▼
┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐
│   DATA LAYER     │ │   QUEUE LAYER    │ │   AI LAYER       │
│ ┌──────────────┐ │ │ ┌──────────────┐ │ │ ┌──────────────┐ │
│ │  Supabase    │ │ │ │   Redis      │ │ │ │  Replicate   │ │
│ │  (Postgres)  │ │ │ │  (BullMQ)    │ │ │ │  (SDXL)      │ │
│ └──────────────┘ │ │ └──────────────┘ │ │ └──────────────┘ │
│ ┌──────────────┐ │ │                  │ │ ┌──────────────┐ │
│ │  Supabase    │ │ │                  │ │ │  OpenAI      │ │
│ │  Storage     │ │ │                  │ │ │  (GPT-4o)    │ │
│ └──────────────┘ │ │                  │ │ └──────────────┘ │
│ ┌──────────────┐ │ │                  │ │                  │
│ │  Supabase    │ │ │                  │ │                  │
│ │  Auth        │ │ │                  │ │                  │
│ └──────────────┘ │ │                  │ │                  │
└──────────────────┘ └──────────────────┘ └──────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      WORKER LAYER                           │
│  ┌─────────────────────────────────────────────────────┐   │
│  │           Python FastAPI Workers                    │   │
│  │  - Image Generation                                 │   │
│  │  - Prompt Enhancement                               │   │
│  │  - Script Parsing                                   │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 2.2 Технологический стек

### Frontend (`apps/web`)

| Технология | Версия | Назначение |
|------------|--------|------------|
| **Next.js** | 14.x | React фреймворк |
| **TypeScript** | 5.x | Типизация |
| **Tailwind CSS** | 4.x | Стилизация |
| **Zustand** | 4.x | State management |
| **shadcn/ui** | latest | UI компоненты |
| **Radix UI** | latest | Примитивы доступности |
| **Lucide React** | latest | Иконки |
| **Axios** | 1.x | HTTP клиент |
| **React Dropzone** | 14.x | Загрузка файлов |
| **@dnd-kit** | 6.x | Drag-n-drop |

### Backend API (`apps/api`)

| Технология | Версия | Назначение |
|------------|--------|------------|
| **Node.js** | 20.x | Runtime |
| **Express** | 4.x | Web фреймворк |
| **TypeScript** | 5.x | Типизация |
| **Supabase** | 2.x | Database + Auth + Storage |
| **Zod** | 3.x | Валидация схем |
| **BullMQ** | 5.x | Очереди задач |
| **ioredis** | 5.x | Redis клиент |
| **Multer** | 1.x | Загрузка файлов |
| **Stripe** | 14.x | Платежи |
| **Winston** | 3.x | Логирование |
| **Helmet** | 7.x | Security headers |
| **CORS** | 2.x | Cross-origin requests |

### AI Workers (`apps/workers`)

| Технология | Версия | Назначение |
|------------|--------|------------|
| **Python** | 3.11 | Runtime |
| **FastAPI** | 0.109+ | Web фреймворк |
| **Pydantic** | 2.x | Валидация данных |
| **Replicate** | 0.22+ | AI изображения |
| **OpenAI** | 1.10+ | LLM для промптов |
| **Redis** | 5.x | Кэширование |
| **RQ** | 1.15+ | Task queues |
| **Pillow** | 10.x | Обработка изображений |
| **HTTPX** | 0.26+ | Async HTTP |

### Инфраструктура

| Сервис | Назначение | Тариф |
|--------|------------|-------|
| **Vercel** | Frontend hosting | Free/Pro |
| **Render** | Backend hosting | Free/$7-25/мес |
| **Supabase** | Database + Auth | Free/$25/мес |
| **Redis Cloud** | Cache + Queues | Free/$10/мес |
| **Replicate** | AI изображения | Pay-per-use |
| **OpenAI** | LLM API | Pay-per-use |
| **Stripe** | Платежи | 2.9% + $0.30 |
| **Cloudflare** | DNS + CDN | Free |

---

## 2.3 Структура проекта

```
storyboard-ai/
│
├── apps/                          # Основные приложения
│   ├── api/                       # Backend API (Express)
│   │   ├── src/
│   │   │   ├── index.ts           # Entry point
│   │   │   ├── routes/            # API endpoints
│   │   │   │   ├── auth.ts        # Аутентификация
│   │   │   │   ├── projects.ts    # Проекты
│   │   │   │   ├── scenes.ts      # Сцены
│   │   │   │   ├── scripts.ts     # Сценарии
│   │   │   │   ├── generation.ts  # Генерация
│   │   │   │   └── subscriptions.ts # Подписки
│   │   │   ├── middleware/        # Промежуточное ПО
│   │   │   │   ├── auth.ts        # Проверка токена
│   │   │   │   ├── validation.ts  # Валидация
│   │   │   │   └── error.ts       # Обработка ошибок
│   │   │   ├── lib/               # Библиотеки
│   │   │   │   ├── supabase.ts    # Supabase клиент
│   │   │   │   ├── redis.ts       # Redis клиент
│   │   │   │   └── local-auth.ts  # Локальная аутентификация
│   │   │   ├── utils/             # Утилиты
│   │   │   │   ├── logger.ts      # Логгер
│   │   │   │   └── errors.ts      # Классы ошибок
│   │   │   └── services/          # Бизнес-логика
│   │   ├── supabase/
│   │   │   └── schema.sql         # Схема БД
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   └── Dockerfile
│   │
│   ├── web/                       # Frontend (Next.js)
│   │   ├── src/
│   │   │   ├── app/               # Next.js страницы
│   │   │   │   ├── layout.tsx     # Корневой layout
│   │   │   │   ├── page.tsx       # Главная страница
│   │   │   │   ├── login/         # Страница входа
│   │   │   │   ├── signup/        # Страница регистрации
│   │   │   │   ├── dashboard/     # Личный кабинет
│   │   │   │   └── projects/      # Проекты
│   │   │   ├── components/        # React компоненты
│   │   │   │   ├── ui/            # Базовые компоненты
│   │   │   │   ├── header.tsx     # Шапка
│   │   │   │   └── auth-redirect.tsx # Редирект авторизации
│   │   │   ├── contexts/          # React контексты
│   │   │   │   ├── theme-context.tsx # Тема
│   │   │   │   └── language-context.tsx # Язык
│   │   │   ├── stores/            # Zustand store
│   │   │   │   ├── auth-store.ts  # Аутентификация
│   │   │   │   └── admin-auth-store.ts # Админ
│   │   │   └── lib/               # Утилиты
│   │   │       ├── utils.ts       # Helper функции
│   │   │       └── translations.ts # Переводы
│   │   ├── public/                # Статические файлы
│   │   ├── package.json
│   │   ├── next.config.ts
│   │   └── tailwind.config.ts
│   │
│   └── workers/                   # AI Workers (Python)
│       ├── src/
│       │   ├── main.py            # FastAPI приложение
│       │   ├── config.py          # Конфигурация
│       │   ├── worker.py          # Фоновые задачи
│       │   ├── routers/           # API роуты
│       │   │   ├── generation.py  # Генерация
│       │   │   ├── prompts.py     # Промпты
│       │   │   └── health.py      # Health check
│       │   └── services/          # AI сервисы
│       │       ├── image_gen.py   # Генерация изображений
│       │       └── prompt_eng.py  # Улучшение промптов
│       ├── pyproject.toml
│       └── Dockerfile
│
├── packages/                      # Общие пакеты
│   └── shared/                    # Общие типы и схемы
│       ├── src/
│       │   └── index.ts           # Zod схемы
│       └── package.json
│
├── .kilocode/                     # Документация проекта
│   ├── instructions.md            # Инструкции для AI
│   ├── patterns.md                # Паттерны кода
│   ├── prompts.md                 # Промпты
│   └── checklist.md               # Чек-листы
│
├── .env.example                   # Пример переменных
├── .env.local                     # Локальные переменные
├── .gitignore                     # Git ignore
├── docker-compose.yml             # Docker конфигурация
├── package.json                   # Корневой package
├── turbo.json                     # Turborepo конфиг
├── README.md                      # Основная документация
├── DEVELOPMENT.md                 # Руководство разработки
├── DEPLOYMENT_PLAN.md             # План развёртывания
├── QUICK_DEPLOY.md                # Быстрый деплой
├── SUPABASE_SETUP.md              # Настройка Supabase
├── DEPLOY_BEGEТ.md                # Деплой на Beget
└── ПЛАН.md                        # План на русском
```

---

## 2.4 База данных

### Схема данных

```sql
-- Основные таблицы

users
├── id (UUID, PK)              # ID пользователя
├── email (TEXT)               # Email
├── name (TEXT)                # Имя
├── avatar_url (TEXT)          # Аватар
├── credits (INT)              # Баланс кредитов
├── subscription_tier (ENUM)   # Тариф
├── created_at (TIMESTAMPTZ)   # Дата создания
└── updated_at (TIMESTAMPTZ)   # Дата обновления

projects
├── id (UUID, PK)              # ID проекта
├── user_id (UUID, FK)         # Владелец
├── title (VARCHAR)            # Название
├── description (TEXT)         # Описание
├── thumbnail_url (TEXT)       # Превью
├── status (ENUM)              # Статус
├── created_at (TIMESTAMPTZ)
└── updated_at (TIMESTAMPTZ)

scenes
├── id (UUID, PK)              # ID сцены
├── project_id (UUID, FK)      # Проект
├── scene_number (INT)         # Номер сцены
├── title (VARCHAR)            # Заголовок
├── description (TEXT)         # Описание
├── dialogue (TEXT)            # Диалог
├── characters (TEXT[])        # Персонажи
├── location (TEXT)            # Локация
├── time_of_day (VARCHAR)      # Время суток
├── camera_angle (VARCHAR)     # Ракурс
├── style (VARCHAR)            # Стиль
├── aspect_ratio (VARCHAR)     # Соотношение сторон
├── image_url (TEXT)           # URL изображения
├── prompt (TEXT)              # Промпт для AI
├── negative_prompt (TEXT)     # Негативный промпт
├── status (ENUM)              # Статус
├── metadata (JSONB)           # Метаданные
├── created_at (TIMESTAMPTZ)
└── updated_at (TIMESTAMPTZ)

scripts
├── id (UUID, PK)              # ID сценария
├── user_id (UUID, FK)         # Владелец
├── filename (TEXT)            # Имя файла
├── file_type (VARCHAR)        # Тип файла
├── file_size (INT)            # Размер
├── storage_path (TEXT)        # Путь в Storage
├── content (TEXT)             # Содержимое
├── parsed_scenes (JSONB)      # Распарсенные сцены
├── status (ENUM)              # Статус
├── created_at (TIMESTAMPTZ)
└── updated_at (TIMESTAMPTZ)

generation_jobs
├── id (UUID, PK)              # ID задачи
├── scene_id (UUID, FK)        # Сцена
├── user_id (UUID, FK)         # Пользователь
├── status (ENUM)              # Статус
├── prompt (TEXT)              # Промпт
├── negative_prompt (TEXT)     # Негативный промпт
├── image_url (TEXT)           # Результат
├── error_message (TEXT)       # Ошибка
├── attempts (INT)             # Попытки
├── started_at (TIMESTAMPTZ)
├── completed_at (TIMESTAMPTZ)
└── created_at (TIMESTAMPTZ)

subscriptions
├── id (UUID, PK)              # ID подписки
├── user_id (UUID, FK)         # Пользователь
├── stripe_subscription_id (TEXT) # ID в Stripe
├── stripe_customer_id (TEXT)  # ID клиента в Stripe
├── tier (ENUM)                # Тариф
├── status (ENUM)              # Статус
├── current_period_start (TIMESTAMPTZ)
├── current_period_end (TIMESTAMPTZ)
├── cancel_at_period_end (BOOLEAN)
├── created_at (TIMESTAMPTZ)
└── updated_at (TIMESTAMPTZ)

credit_transactions
├── id (UUID, PK)              # ID транзакции
├── user_id (UUID, FK)         # Пользователь
├── amount (INT)               # Сумма
├── type (ENUM)                # Тип
├── description (TEXT)         # Описание
├── balance_after (INT)        # Баланс после
├── metadata (JSONB)           # Метаданные
└── created_at (TIMESTAMPTZ)
```

### ENUM типы

```sql
subscription_tier: 'free' | 'basic' | 'pro' | 'enterprise'
subscription_status: 'active' | 'canceled' | 'past_due' | 'trialing'
project_status: 'draft' | 'processing' | 'completed' | 'failed'
scene_status: 'pending' | 'generating' | 'completed' | 'failed'
script_status: 'uploaded' | 'parsing' | 'parsed' | 'failed'
generation_job_status: 'queued' | 'processing' | 'completed' | 'failed'
credit_transaction_type: 'grant' | 'purchase' | 'usage' | 'refund' | 'expiration'
```

### Индексы

```sql
-- Для ускорения запросов
idx_users_email ON users(email)
idx_users_subscription_tier ON users(subscription_tier)
idx_projects_user_id ON projects(user_id)
idx_projects_status ON projects(status)
idx_scenes_project_id ON scenes(project_id)
idx_scenes_status ON scenes(status)
idx_generation_jobs_scene_id ON generation_jobs(scene_id)
idx_generation_jobs_status ON generation_jobs(status)
idx_subscriptions_user_id ON subscriptions(user_id)
idx_credit_transactions_user_id ON credit_transactions(user_id)
```

---

## 2.5 API Endpoints

### Authentication

| Метод | Endpoint | Описание | Auth |
|-------|----------|----------|------|
| POST | `/api/auth/signup` | Регистрация | ❌ |
| POST | `/api/auth/login` | Вход | ❌ |
| POST | `/api/auth/logout` | Выход | ✅ |
| GET | `/api/auth/me` | Текущий пользователь | ✅ |

### Projects

| Метод | Endpoint | Описание | Auth |
|-------|----------|----------|------|
| GET | `/api/projects` | Список проектов | ✅ |
| POST | `/api/projects` | Создать проект | ✅ |
| GET | `/api/projects/:id` | Детали проекта | ✅ |
| PUT | `/api/projects/:id` | Обновить проект | ✅ |
| DELETE | `/api/projects/:id` | Удалить проект | ✅ |

### Scenes

| Метод | Endpoint | Описание | Auth |
|-------|----------|----------|------|
| GET | `/api/scenes/project/:projectId` | Сцены проекта | ✅ |
| POST | `/api/scenes` | Создать сцену | ✅ |
| PUT | `/api/scenes/:id` | Обновить сцену | ✅ |
| DELETE | `/api/scenes/:id` | Удалить сцену | ✅ |

### Scripts

| Метод | Endpoint | Описание | Auth |
|-------|----------|----------|------|
| POST | `/api/scripts/upload` | Загрузить сценарий | ✅ |
| POST | `/api/scripts/:id/parse` | Парсить сценарий | ✅ |
| GET | `/api/scripts/:id` | Получить сценарий | ✅ |
| DELETE | `/api/scripts/:id` | Удалить сценарий | ✅ |

### Generation

| Метод | Endpoint | Описание | Auth |
|-------|----------|----------|------|
| POST | `/api/generation/image` | Генерировать изображение | ✅ |
| GET | `/api/generation/:jobId` | Статус генерации | ✅ |
| POST | `/api/generation/:jobId/regenerate` | Перегенерировать | ✅ |

### Subscriptions

| Метод | Endpoint | Описание | Auth |
|-------|----------|----------|------|
| GET | `/api/subscriptions/me` | Текущая подписка | ✅ |
| POST | `/api/subscriptions/checkout` | Создать checkout | ✅ |
| POST | `/api/subscriptions/webhook` | Stripe webhook | ❌ |

---

## 2.6 Компоненты системы

### Frontend компоненты

#### Страницы

| Страница | Путь | Описание |
|----------|------|----------|
| Landing | `/` | Главная страница |
| Login | `/login` | Вход |
| Signup | `/signup` | Регистрация |
| Dashboard | `/dashboard` | Личный кабинет |
| Project Editor | `/projects/[id]` | Редактор проекта |
| Admin Login | `/admin/login` | Вход для админа |
| Admin Panel | `/admin` | Панель админа |

#### Компоненты UI

```typescript
// Базовые компоненты
Button          // Кнопка
Input           // Поле ввода
Label           // Метка
Card            // Карточка
Textarea        // Многострочное поле
Select          // Выпадающий список
Switch          // Переключатель

// Специализированные
Header          // Шапка сайта
AuthRedirect    // Проверка авторизации
LanguageSwitcher // Переключатель языка
ThemeSwitcher   // Переключатель темы
```

### Backend сервисы

#### API сервисы

```typescript
// apps/api/src/services/

AuthService       // Аутентификация
ProjectService    // Управление проектами
SceneService      // Управление сценами
ScriptService     // Обработка сценариев
GenerationService // Генерация изображений
SubscriptionService // Подписки
PaymentService    // Платежи
```

#### AI сервисы

```python
# apps/workers/src/services/

ImageGenerator    # Генерация изображений
PromptEngineer    # Улучшение промптов
ScriptParser      # Парсинг сценариев
CharacterEmbedding # Встраивание персонажей
```

---

## 2.7 Безопасность

### Уровни защиты

```
┌─────────────────────────────────────────┐
│           CLIENT LAYER                  │
│  - Input validation                     │
│  - XSS prevention (React)               │
│  - CSRF tokens                          │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│           API LAYER                     │
│  - JWT authentication                   │
│  - Rate limiting                        │
│  - CORS policy                          │
│  - Helmet security headers              │
│  - Input validation (Zod)               │
│  - SQL injection prevention             │
└─────────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────┐
│           DATABASE LAYER                │
│  - Row Level Security (RLS)             │
│  - Parameterized queries                │
│  - Encrypted connections (SSL)          │
│  - Backup encryption                    │
└─────────────────────────────────────────┘
```

### Меры безопасности

1. **Аутентификация**
   - JWT токены (1 час жизни)
   - Refresh токены
   - Хеширование паролей (SHA-256)

2. **Авторизация**
   - RLS политики в Supabase
   - Проверка владения ресурсами
   - Ролевая модель (user/admin)

3. **Валидация**
   - Zod схемы на входе API
   - Валидация MIME типов файлов
   - Ограничение размера файлов (10MB)

4. **Сетевая безопасность**
   - HTTPS обязательно
   - CORS для доменов
   - Rate limiting (100 запросов/мин)

5. **Данные**
   - Шифрование при передаче (TLS)
   - Шифрование при хранении
   - Регулярные бэкапы

---

## 2.8 Развёртывание

### Переменные окружения

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# AI Services
REPLICATE_API_KEY=
OPENAI_API_KEY=

# Stripe
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

# Redis
REDIS_URL=redis://localhost:6379

# Application
NODE_ENV=production
NEXT_PUBLIC_APP_URL=https://domain.com
NEXT_PUBLIC_API_URL=https://api.domain.com
API_PORT=4000
JWT_SECRET=<random-64-chars>
```

### Docker Compose

```yaml
version: '3.8'

services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  api:
    build: ./apps/api
    ports:
      - "4000:4000"
    environment:
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis

  workers:
    build: ./apps/workers
    environment:
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis

volumes:
  redis_data:
```

### Команды развёртывания

```bash
# Локальная разработка
npm install
npm run dev

# Сборка
npm run build

# Тесты
npm test

# Docker
docker-compose up -d

# Деплой frontend
cd apps/web && vercel --prod

# Деплой backend
cd apps/api && render deploy
```

---

## 2.9 Мониторинг и логи

### Логирование

```typescript
// Уровни логирования
logger.error()  // Критические ошибки
logger.warn()   // Предупреждения
logger.info()   // Информация
logger.debug()  // Отладка (dev только)
```

### Метрики для отслеживания

| Метрика | Цель | Порог тревоги |
|---------|------|---------------|
| Uptime | >99.9% | <99% |
| API Response Time | <200ms | >500ms |
| Image Generation | <60s | >120s |
| Error Rate | <1% | >5% |
| Active Users | Рост | Падение >20% |

### Инструменты мониторинга

- **Sentry** — отслеживание ошибок
- **UptimeRobot** — мониторинг доступности
- **Google Analytics** — аналитика пользователей
- **Logtail** — агрегация логов

---

## 2.10 Тестирование

### Типы тестов

```
1. Unit тесты
   - Тестирование отдельных функций
   - Покрытие: >80%

2. Integration тесты
   - Взаимодействие компонентов
   - API endpoints

3. E2E тесты
   - Полные пользовательские сценарии
   - Критические пути
```

### Команды тестирования

```bash
# Frontend тесты
cd apps/web && npm test

# API тесты
cd apps/api && npm test

# Workers тесты
cd apps/workers && pytest
```

---

## 2.11 Производительность

### Целевые показатели

| Показатель | Цель |
|------------|------|
| First Contentful Paint | <1.5s |
| Time to Interactive | <3.5s |
| Lighthouse Score | >90 |
| API P95 Latency | <200ms |
| Image Generation | <60s |

### Оптимизации

1. **Frontend**
   - Static generation (SSG)
   - Image optimization (WebP)
   - Code splitting
   - Lazy loading

2. **Backend**
   - Database indexing
   - Redis caching
   - Query optimization
   - Connection pooling

3. **AI**
   - Prompt caching
   - Batch generation
   - Retry logic
   - Queue management

---

## 📞 Поддержка и контакты

### Документация
- README.md — основная документация
- DEVELOPMENT.md — руководство разработки
- DEPLOYMENT_PLAN.md — план развёртывания
- SUPABASE_SETUP.md — настройка Supabase

### Ресурсы
- GitHub: https://github.com/yourusername/storyboard-ai
- Email: support@storyboardai.com
- Docs: https://docs.storyboardai.com

---

**Made with ❤️ by Storyboard AI Team**

*Последнее обновление: 20 февраля 2026*
