# 🚀 Storyboard AI — Полный деплой на VPS Beget

> **Дата создания:** 23 февраля 2026  
> **Статус:** ✅ Проверено и работает  
> **Время на выполнение:** 2-3 часа

---

## 📋 Содержание

1. [Требования](#требования)
2. [Заказ VPS на Beget](#заказ-vps-на-beget)
3. [Подключение к серверу](#подключение-к-серверу)
4. [Настройка сервера](#настройка-сервера)
5. [Установка ПО](#установка-по)
6. [Загрузка проекта](#загрузка-проекта)
7. [Настройка проекта](#настройка-проекта)
8. [Запуск через PM2](#запуск-через-pm2)
9. [Настройка домена](#настройка-домена)
10. [Настройка HTTPS](#настройка-https)
11. [Проверка работы](#проверка-работы)
12. [Частые проблемы](#частые-проблемы)

---

## Требования

| Что | Где взять | Стоимость |
|-----|-----------|-----------|
| VPS сервер | Beget → раздел VPS | от 450 руб/мес |
| Домен (опционально) | Beget → раздел Домены | от 199 руб/год |
| SSH доступ | Приходит на email после заказа VPS | Бесплатно |

**Рекомендуемая конфигурация VPS:**
- CPU: 2 ядра
- RAM: 2 GB
- SSD: 25 GB
- ОС: Ubuntu 22.04 LTS
- Root-доступ: включён

---

## Заказ VPS на Beget

1. Зайдите на https://beget.com
2. Войдите в аккаунт (или зарегистрируйтесь)
3. Перейдите в раздел **«VPS»** в верхнем меню
4. Нажмите **«Заказать VPS»**
5. Выберите конфигурацию:
   - ОС: **Ubuntu 22.04 LTS**
   - CPU: **2 ядра**
   - RAM: **2 GB**
   - SSD: **25 GB**
   - Root-доступ: **включён**
6. Оплатите тариф (≈450 руб/мес)
7. Через 5-15 минут на email придут данные:
   - IP-адрес сервера
   - Логин: `root`
   - Пароль

---

## Подключение к серверу

### Через PowerShell (Windows)

```powershell
ssh root@ВАШ_IP_СЕРВЕРА
```

Пример:
```powershell
ssh root@45.141.79.111
```

- На вопрос `yes/no` введите: **yes**
- Введите пароль (символы не отображаются — это нормально!)

### Через PuTTY

1. Скачайте: https://www.putty.org
2. Host Name: `ВАШ_IP_СЕРВЕРА`
3. Port: `22`
4. Нажмите **Open**
5. Логин: `root`
6. Введите пароль

---

## Настройка сервера

### Смените пароль (обязательно!)

```bash
passwd
```

Введите текущий пароль → новый пароль (минимум 12 символов) → повторите.

### Обновите систему

```bash
apt update && apt upgrade -y
```

Ждите 2-5 минут до завершения.

---

## Установка ПО

Выполняйте команды по порядку:

```bash
# Node.js 20.x
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs

# Git
apt install -y git

# Redis
apt install -y redis-server
systemctl enable redis-server
systemctl start redis-server

# Python venv (для workers)
apt install -y python3-venv

# Nginx
apt install -y nginx
systemctl enable nginx
systemctl start nginx

# PM2 (менеджер процессов)
npm install -g pm2

# Certbot (для HTTPS)
apt install -y certbot python3-certbot-nginx
```

### Проверка установки

```bash
node -v    # должно быть v20.x.x
npm -v     # должно быть 10.x.x
git --version
redis-cli ping   # должно вернуть: PONG
pm2 -v
```

---

## Загрузка проекта

### Создайте пользователя для приложения

```bash
adduser storyboard
```

Придумайте пароль (введите дважды). Остальные поля — просто Enter.

### Переключитесь на пользователя

```bash
su - storyboard
```

### Склонируйте проект с GitHub

**Если проект публичный:**
```bash
cd /home/storyboard
git clone https://github.com/ВАШ_НИК/storyboard-ai.git
cd storyboard-ai
```

**Если проект приватный:**
```bash
cd /home/storyboard
git clone https://ВАШ_ТОКЕН@github.com/ВАШ_НИК/storyboard-ai.git
cd storyboard-ai
```

> Токен создаётся на GitHub: Settings → Developer settings → Personal access tokens → права `repo`

---

## Настройка проекта

### Установка зависимостей

```bash
# Корневые зависимости
npm install

# API зависимости
cd apps/api
npm install
npm run build
cd ../..

# Web зависимости
cd apps/web
npm install
npm run build
cd ../..
```

### Python зависимости (workers)

```bash
cd apps/workers
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install fastapi uvicorn python-dotenv replicate openai redis pydantic pydantic-settings httpx
deactivate
cd ../..
```

### Настройка .env.local

```bash
cd /home/storyboard/storyboard-ai
nano .env.local
```

**Вставьте содержимое:**

```env
NEXT_PUBLIC_SUPABASE_URL=https://ВАШ-ПРОЕКТ.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ВАШ_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY=ВАШ_SERVICE_ROLE_KEY

REPLICATE_API_KEY=r8_ВАШ_КЛЮЧ
OPENAI_API_KEY=sk-ВАШ_КЛЮЧ

STRIPE_SECRET_KEY=sk_test_ВАШ_КЛЮЧ
STRIPE_PUBLISHABLE_KEY=pk_test_ВАШ_КЛЮЧ
STRIPE_WEBHOOK_SECRET=whsec_ВАШ_СЕКРЕТ

REDIS_URL=redis://localhost:6379

NODE_ENV=production
NEXT_PUBLIC_APP_URL=http://ВАШ_IP_ИЛИ_ДОМЕН
NEXT_PUBLIC_API_URL=http://ВАШ_IP_ИЛИ_ДОМЕН:4000

API_PORT=4000
API_URL=http://localhost:4000
WORKERS_URL=http://localhost:8000
JWT_SECRET=СГЕНЕРИРУЙТЕ_КОМАНДОЙ_НИЖЕ

MAX_FILE_SIZE_MB=10
MAX_SCENES_PER_PROJECT=100
MAX_CONCURRENT_GENERATIONS=3

DEBUG_AI=false
LOG_LEVEL=info
```

**Для генерации JWT_SECRET** (в новом окне PowerShell на ПК):
```powershell
-join ((65..90) + (97..122) + (48..57) | Get-Random -Count 64 | ForEach-Object {[char]$_})
```

**Сохраните файл:**
- `Ctrl + O` → `Enter` (сохранить)
- `Ctrl + X` (выйти)

---

## Запуск через PM2

### Запустите API

```bash
cd /home/storyboard/storyboard-ai/apps/api
pm2 start dist/index.js --name storyboard-api
cd ../..
```

### Запустите Web (frontend)

```bash
cd /home/storyboard/storyboard-ai/apps/web
pm2 start npm --name storyboard-web -- start
cd ../..
```

### Запустите Workers

```bash
cd /home/storyboard/storyboard-ai/apps/workers

# Создайте скрипт запуска
nano run_workers.sh
```

**Вставьте содержимое:**
```bash
#!/bin/bash
cd /home/storyboard/storyboard-ai/apps/workers
source venv/bin/activate
cd src
python -m uvicorn main:app --host 0.0.0.0 --port 8000
```

**Сохраните:** `Ctrl+O` → `Enter` → `Ctrl+X`

```bash
# Сделайте скрипт исполняемым
chmod +x run_workers.sh

# Запустите через PM2
pm2 start run_workers.sh --name storyboard-workers

cd ../..
```

### Проверьте статус

```bash
pm2 status
```

Все сервисы должны быть `online`.

### Сохраните для автозапуска

```bash
pm2 save
pm2 startup
```

**Последняя команда покажет команду для копирования — выполните её!**

---

## Настройка домена

### Шаг 1: Купите домен

1. В панели Beget перейдите в раздел **«Домены»**
2. Нажмите **«Зарегистрировать домен»**
3. Введите имя (например: `storyboard-ai.ru`)
4. Оплатите (≈199 руб/год)

### Шаг 2: Настройте DNS

В панели Beget → Домены → ваш домен → **DNS**:

**Добавьте A-запись:**
- **Subdomain:** `@` (или оставьте пустым)
- **Type:** `A`
- **IP Address:** `ВАШ_IP_СЕРВЕРА`

**Добавьте ещё одну для www:**
- **Subdomain:** `www`
- **Type:** `A`
- **IP Address:** `ВАШ_IP_СЕРВЕРА`

**TTL:** автоматически (обычно 3600)

### Шаг 3: Подождите обновление DNS

**От 15 минут до 24 часов.**

Проверить: https://dnschecker.org

---

## Настройка HTTPS

### Шаг 1: Создайте конфиг Nginx

```bash
# Выйдите из пользователя storyboard
exit

# Создайте конфиг
sudo nano /etc/nginx/sites-available/storyboard
```

**Вставьте содержимое** (замените `ВАШ_ДОМЕН.RU` на ваш домен):

```nginx
server {
    listen 80;
    server_name ВАШ_ДОМЕН.RU www.ВАШ_ДОМЕН.RU;

    client_max_body_size 10M;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    location /_next/static {
        proxy_pass http://localhost:3000;
        proxy_cache_valid 60m;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }
}
```

**Сохраните:** `Ctrl+O` → `Enter` → `Ctrl+X`

### Шаг 2: Включите конфиг

```bash
# Создайте символическую ссылку
sudo ln -s /etc/nginx/sites-available/storyboard /etc/nginx/sites-enabled/

# Удалите стандартный конфиг
sudo rm /etc/nginx/sites-enabled/default

# Проверьте конфигурацию
sudo nginx -t

# Перезапустите Nginx
sudo systemctl restart nginx
```

### Шаг 3: Получите SSL сертификат

```bash
sudo certbot --nginx -d ВАШ_ДОМЕН.RU -d www.ВАШ_ДОМЕН.RU
```

**Вас спросят:**
1. **Email** — введите ваш email
2. **Terms of Service** — введите `A` (согласен)
3. **Newsletter** — `Y` или `N` (по желанию)
4. **Redirect HTTP → HTTPS** — введите `2` (рекомендуется)

---

## Проверка работы

### Проверьте сервисы

```bash
# Вернитесь к пользователю storyboard
su - storyboard

# Проверьте статус
pm2 status
```

Все должны быть `online`.

### Проверьте в браузере

- **Без домена:** `http://ВАШ_IP:3000`
- **С доменом:** `https://ВАШ_ДОМЕН.RU`

### Проверьте API

```bash
curl http://localhost:4000/health
curl http://localhost:8000/health
```

---

## Частые проблемы

### Web не работает — ошибка сборки

**Ошибка:** `Cannot find module 'lightningcss'` или `Cannot find native binding`

**Решение:**
```bash
cd /home/storyboard/storyboard-ai/apps/web
npm install @tailwindcss/oxide-linux-x64-gnu
npm run build
pm2 restart storyboard-web
```

### Workers падает — ошибка pydantic_settings

**Ошибка:** `ModuleNotFoundError: No module named 'pydantic_settings'`

**Решение:**
```bash
cd /home/storyboard/storyboard-ai/apps/workers
source venv/bin/activate
pip install pydantic-settings
deactivate
pm2 restart storyboard-workers
```

### Nginx 502 Bad Gateway

**Проверьте:**
```bash
pm2 status
sudo tail -f /var/log/nginx/error.log
```

**Перезапустите сервисы:**
```bash
pm2 restart all
sudo systemctl restart nginx
```

### Redis не работает

```bash
systemctl status redis-server
systemctl restart redis-server
```

---

## Управление проектом

### Просмотр логов

```bash
pm2 logs
pm2 logs storyboard-api
pm2 logs storyboard-web
pm2 logs storyboard-workers
```

**Выход:** `Ctrl + C`

### Перезапуск

```bash
pm2 restart all
pm2 restart storyboard-api
pm2 restart storyboard-web
pm2 restart storyboard-workers
```

### Остановка

```bash
pm2 stop all
```

### Обновление проекта

```bash
su - storyboard
cd /home/storyboard/storyboard-ai
git pull
npm run build
pm2 restart all
exit
```

---

## 💰 Итоговая стоимость

| Услуга | Стоимость |
|--------|-----------|
| VPS Beget (2 ядра, 2 GB) | 450 руб/мес |
| Домен .RU | 199 руб/год |
| Supabase (бесплатный тариф) | 0 руб |
| **Итого в месяц** | **~470 руб** |

---

## ✅ Финальный чек-лист

```
[ ] VPS заказан и активирован
[ ] Подключение по SSH работает
[ ] Node.js установлен (node -v)
[ ] Redis установлен и запущен (redis-cli ping → PONG)
[ ] Nginx установлен
[ ] PM2 установлен
[ ] Проект склонирован с GitHub
[ ] Зависимости установлены
[ ] .env.local создан и заполнен
[ ] API запущен (pm2 status → online)
[ ] Web запущен и собран (npm run build → success)
[ ] Workers запущен
[ ] pm2 save выполнен
[ ] pm2 startup выполнен
[ ] Сайт открывается в браузере
[ ] HTTPS настроен (если есть домен)
```

---

## 📞 Полезные ссылки

- **Панель Beget:** https://beget.com
- **Поддержка Beget:** https://beget.com/support
- **Документация PM2:** https://pm2.keymetrics.io
- **Supabase:** https://supabase.com
- **Replicate AI:** https://replicate.com
- **OpenAI:** https://platform.openai.com

---

## 🎉 Поздравляем!

Ваш Storyboard AI теперь работает в интернете!

**Следующие шаги:**
1. Настройте Supabase (выполните миграцию БД в Supabase Dashboard)
2. Зарегистрируйтесь на сайте через форму регистрации
3. Протестируйте генерацию раскадровок
4. Настройте платежи через Stripe (опционально)

---

*Инструкция создана: 23 февраля 2026*  
*Проверено и работает ✅*
