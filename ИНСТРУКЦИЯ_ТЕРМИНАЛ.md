# 🚀 Деплой на VPS Beget через терминал — Инструкция для новичка

> **Дата:** 23 февраля 2026  
> **Стиль:** Только команды для копирования  
> **Время:** 2-3 часа

---

## 📋 Быстрый чек-лист

```
✅ 1. Заказать VPS на beget.com (2 ядра, 2 GB RAM, Ubuntu 22.04)
✅ 2. Получить IP, логин, пароль на email
✅ 3. Выполнять команды ниже по порядку
```

---

## 🔹 ЧАСТЬ 1: Подключение к серверу (Windows PowerShell)

### Откройте PowerShell

Нажмите `Win + R` → введите `powershell` → нажмите Enter

### Подключение к серверу

```powershell
ssh root@ВАШ_IP_СЕРВЕРА
```

Пример:
```powershell
ssh root@185.123.45.67
```

На вопрос `yes/no` введите: **yes**

Введите пароль (символы не отображаются — это нормально!)

---

## 🔹 ЧАСТЬ 2: Первоначальная настройка сервера

### Смените пароль (обязательно!)

```bash
passwd
```

Введите текущий пароль → новый пароль → повторите новый пароль

### Обновите систему

```bash
apt update && apt upgrade -y
```

Ждите 2-5 минут до завершения.

---

## 🔹 ЧАСТЬ 3: Установка всего необходимого

Выполняйте команды по очереди:

```bash
# Node.js 20.x
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs

# Git
apt install -y git

# Redis
apt install -y redis-server
systemctl enable redis-server
systemctl start redis-server

# Nginx
apt install -y nginx
systemctl enable nginx
systemctl start nginx

# PM2 (менеджер процессов)
npm install -g pm2

# Certbot (для HTTPS)
apt install -y certbot python3-certbot-nginx
```

### Проверка установки

```bash
node -v    # должно быть v20.x.x
npm -v     # должно быть 10.x.x
git --version
redis-cli ping   # должно вернуть: PONG
pm2 -v
```

---

## 🔹 ЧАСТЬ 4: Загрузка проекта

### Создайте пользователя для приложения

```bash
adduser storyboard
```

Придумайте пароль (введите дважды). Остальное — просто Enter.

### Переключитесь на пользователя

```bash
su - storyboard
```

### Склонируйте проект с GitHub

**Если проект публичный:**
```bash
cd /home/storyboard
git clone https://github.com/denkmv21-hash/storyboard-ai.git
cd storyboard-ai
```

**Если проект приватный:**
```bash
cd /home/storyboard
git clone https://ВАШ_ТОКЕН@github.com/ВАШ_НИК/storyboard-ai.git
cd storyboard-ai
```

> Токен создаётся на GitHub: Settings → Developer settings → Personal access tokens → права `repo`

---

## 🔹 ЧАСТЬ 5: Установка зависимостей

```bash
# Корневые зависимости
npm install

# API зависимости
cd apps/api
npm install
npm run build
cd ../..

# Web зависимости
cd apps/web
npm install
npm run build
cd ../..

# Python зависимости (workers)
cd apps/workers
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
deactivate
cd ../..
```

---

## 🔹 ЧАСТЬ 6: Настройка переменных окружения

### Создайте файл .env.local

```bash
cd /home/storyboard/storyboard-ai
nano .env.local
```

### &&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&&Вставьте содержимое

Скопируйте и вставьте в редактор (правой кнопкой мыши в PowerShell):

```env
NEXT_PUBLIC_SUPABASE_URL=https://danimak-studio.ru
NEXT_PUBLIC_SUPABASE_ANON_KEY=ВАШ_ANON_KEY
SUPABASE_SERVICE_ROLE_KEY=ВАШ_SERVICE_ROLE_KEY

REPLICATE_API_KEY=r8_ВАШ_КЛЮЧ
OPENAI_API_KEY=sk-ВАШ_КЛЮЧ

STRIPE_SECRET_KEY=sk_test_ВАШ_КЛЮЧ
STRIPE_PUBLISHABLE_KEY=pk_test_ВАШ_КЛЮЧ
STRIPE_WEBHOOK_SECRET=whsec_ВАШ_СЕКРЕТ

REDIS_URL=redis://localhost:6379

NODE_ENV=production
NEXT_PUBLIC_APP_URL=http://ВАШ_IP
NEXT_PUBLIC_API_URL=http://ВАШ_IP:4000

API_PORT=4000
API_URL=http://localhost:4000
WORKERS_URL=http://localhost:8000
JWT_SECRET=СГЕНЕРИРУЙТЕ_КОМАНДОЙ_НИЖЕ

MAX_FILE_SIZE_MB=10
MAX_SCENES_PER_PROJECT=100
MAX_CONCURRENT_GENERATIONS=3

DEBUG_AI=false
LOG_LEVEL=info
```

### Сгенерируйте JWT_SECRET

Откройте **новый окно PowerShell** (не закрывая текущее) и выполните:

```powershell
openssl rand -base64 48
```

Скопируйте вывод и вставьте в `.env.local` вместо `СГЕНЕРИРУЙТЕ_КОМАНДОЙ_НИЖЕ`

### Сохраните файл

В редакторе nano:
- `Ctrl + O` → `Enter` (сохранить)
- `Ctrl + X` (выйти)

---

## 🔹 ЧАСТЬ 7: Запуск проекта через PM2

### Вернитесь в директорию проекта (если вышли)

```bash
su - storyboard
cd /home/storyboard/storyboard-ai
```

### Запустите API

```bash
cd apps/api
pm2 start dist/index.js --name storyboard-api
cd ../..
```

### Запустите Web (frontend)

```bash
cd apps/web
pm2 start npm --name storyboard-web -- start
cd ../..
```

### Запустите Workers

```bash
cd apps/workers
source venv/bin/activate
pm2 start src/main.py --name storyboard-workers --interpreter python3
deactivate
cd ../..
```

### Проверьте статус

```bash
pm2 status
```

Все сервисы должны быть `online`.

### Сохраните для автозапуска

```bash
pm2 save
pm2 startup
```

Последняя команда покажет команду для копирования — **скопируйте и выполните её!**

---

## 🔹 ЧАСТЬ 8: Проверка локально

```bash
# Проверка API
curl http://localhost:4000/health

# Проверка Web
curl http://localhost:3000
```

Если видите ответ — всё работает!

---

## 🔹 ЧАСТЬ 9: Настройка домена (опционально)

### Если домен есть — настройте DNS в панели Beget

```
A-запись: @ → ВАШ_IP
A-запись: api → ВАШ_IP
```

Подождите 30-60 минут.

### Настройте Nginx

```bash
sudo nano /etc/nginx/sites-available/storyboard
```

Вставьте (замените `ВАШ_ДОМЕН.RU` на ваш домен):

```nginx
server {
    listen 80;
    server_name ВАШ_ДОМЕН.RU www.ВАШ_ДОМЕН.RU;

    client_max_body_size 10M;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

Сохраните: `Ctrl + O` → `Enter` → `Ctrl + X`

### Для API

```bash
sudo nano /etc/nginx/sites-available/api
```

Вставьте:

```nginx
server {
    listen 80;
    server_name api.ВАШ_ДОМЕН.RU;

    location / {
        proxy_pass http://localhost:4000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

Сохраните: `Ctrl + O` → `Enter` → `Ctrl + X`

### Включите конфигурации

```bash
sudo ln -s /etc/nginx/sites-available/storyboard /etc/nginx/sites-enabled/
sudo ln -s /etc/nginx/sites-available/api /etc/nginx/sites-enabled/
sudo rm /etc/nginx/sites-enabled/default
sudo nginx -t
sudo systemctl restart nginx
```

---

## 🔹 ЧАСТЬ 10: Настройка HTTPS

```bash
# Для основного домена
sudo certbot --nginx -d ВАШ_ДОМЕН.RU -d www.ВАШ_ДОМЕН.RU

# Для API
sudo certbot --nginx -d api.ВАШ_ДОМЕН.RU
```

Введите email для уведомлений → согласитесь с условиями → готово!

---

## 🔹 ЧАСТЬ 11: Настройка фаервола

```bash
# Выйдите из пользователя storyboard
exit

# Установите и настройте UFW
apt install -y ufw
ufw allow OpenSSH
ufw allow 'Nginx Full'
ufw enable
ufw status
```

На вопрос `y/n` введите: **y**

---

## 🔹 ЧАСТЬ 12: Финальная проверка

### Проверьте сервисы

```bash
pm2 status
```

Все должны быть `online`.

### Проверьте в браузере

Откройте:
- **Без домена:** `http://ВАШ_IP:3000`
- **С доменом:** `https://ВАШ_ДОМЕН.RU`

---

## 📋 Управление проектом

### Просмотр логов

```bash
pm2 logs
```

Выход: `Ctrl + C`

### Перезапуск

```bash
pm2 restart all
```

### Остановка

```bash
pm2 stop all
```

### Обновление проекта

```bash
su - storyboard
cd /home/storyboard/storyboard-ai
git pull
npm run build
pm2 restart all
exit
```

---

## ⚠️ Решение проблем

### Сервис упал

```bash
pm2 logs storyboard-api --lines 50
```

### Ошибка Redis

```bash
systemctl status redis-server
systemctl restart redis-server
```

### Ошибка "Cannot find module"

```bash
cd /home/storyboard/storyboard-ai/apps/api
rm -rf node_modules
npm install
npm run build
pm2 restart storyboard-api
```

### Nginx 502 Bad Gateway

```bash
pm2 status
sudo tail -f /var/log/nginx/error.log
```

---

## 💰 Стоимость

| Услуга | Цена |
|--------|------|
| VPS Beget (2 ядра, 2 GB) | 450 руб/мес |
| Домен .RU | 199 руб/год |
| **Итого** | **~470 руб/мес** |

---

## ✅ Финальный чек-лист

```
[ ] VPS заказан
[ ] Подключение по SSH работает
[ ] Node.js установлен
[ ] Redis запущен
[ ] Проект склонирован
[ ] .env.local создан
[ ] PM2 сервисы запущены
[ ] pm2 save выполнен
[ ] Сайт открывается
[ ] HTTPS настроен (если есть домен)
```

---

**Готово! Ваш сайт работает в интернете! 🎉**
