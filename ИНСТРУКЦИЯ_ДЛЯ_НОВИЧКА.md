# 🚀 Перенос сайта с GitHub на VPS Beget — Инструкция для новичка

> **Дата обновления:** 23 февраля 2026  
> **Уровень:** Для начинающих  
> **Время на выполнение:** 2-3 часа

---

## 📋 Что вам понадобится

| Что | Где взять | Стоимость |
|-----|-----------|-----------|
| Аккаунт на GitHub | github.com | Бесплатно |
| Аккаунт на Beget | beget.com | Бесплатная регистрация |
| VPS сервер | Beget → раздел VPS | от 450 руб/мес |
| Домен (по желанию) | Beget → раздел Домены | от 199 руб/год |

---

## 📖 Оглавление

1. [Заказ VPS сервера на Beget](#этап-1-заказ-vps-сервера-на-beget)
2. [Подключение к серверу через SSH](#этап-2-подключение-к-серверу-через-ssh)
3. [Установка программ на сервер](#этап-3-установка-программ-на-сервер)
4. [Загрузка проекта с GitHub](#этап-4-загрузка-проекта-с-github)
5. [Настройка проекта](#этап-5-настройка-проекта)
6. [Запуск проекта](#этап-6-запуск-проекта)
7. [Настройка домена и HTTPS](#этап-7-настройка-домена-и-https)
8. [Проверка работы](#этап-8-проверка-работы)
9. [Частые проблемы](#частые-проблемы)

---

## Этап 1: Заказ VPS сервера на Beget

### Шаг 1.1: Регистрация на Beget

1. Откройте https://beget.com
2. Нажмите **«Регистрация»** в правом верхнем углу
3. Заполните форму (email, пароль, телефон)
4. Подтвердите email и телефон

### Шаг 1.2: Заказ VPS

1. В личном кабинете нажмите **«VPS»** в верхнем меню
2. Нажмите кнопку **«Заказать VPS»**
3. Выберите конфигурацию (рекомендуется для новичка):

```
✅ Операционная система: Ubuntu 22.04 LTS
✅ CPU: 2 ядра
✅ RAM: 2 GB
✅ SSD: 25 GB
✅ Root-доступ: ВКЛЮЧЁН (обязательно!)
```

4. Нажмите **«Заказать»**
5. Оплатите тариф (≈450 руб/мес)

### Шаг 1.3: Получение данных сервера

Через 5-15 минут сервер будет готов. Вы получите email с данными:

```
📌 IP-адрес: 123.45.67.89
📌 Логин: root
📌 Пароль: ваш-пароль-123
```

**Сохраните эти данные!** Они понадобятся для подключения.

---

## Этап 2: Подключение к серверу через SSH

### Вариант A: Через PowerShell (Windows 10/11)

1. Нажмите `Win + X` → выберите **Windows PowerShell** или **Терминал**
2. Введите команду:

```bash
ssh root@123.45.67.89
```

3. Введите `yes` когда спросит про fingerprint
4. Введите пароль (символы не будут отображаться — это нормально!)
5. Нажмите Enter

### Вариант B: Через PuTTY

1. Скачайте PuTTY: https://www.putty.org
2. Запустите программу
3. В поле **Host Name** введите: `123.45.67.89`
4. Port: `22`
5. Нажмите **Open**
6. Введите логин: `root`
7. Введите пароль

### Вариант C: Через Termius (рекомендуется для новичков)

1. Скачайте Termius: https://termius.com/download
2. Установите и запустите
3. Нажмите **New Host**
4. Введите:
   - **Alias:** Storyboard Server
   - **Hostname:** 123.45.67.89
   - **Username:** root
   - **Password:** ваш-пароль
5. Сохраните и нажмите на сервер для подключения

---

### Шаг 2.1: Смена пароля (ОБЯЗАТЕЛЬНО!)

После первого подключения смените пароль:

```bash
passwd
```

1. Введите текущий пароль
2. Введите новый пароль (минимум 12 символов)
3. Повторите новый пароль

**Запомните новый пароль!**

---

### Шаг 2.2: Обновление системы

```bash
apt update && apt upgrade -y
```

Это займёт 2-5 минут. Дождитесь завершения.

---

## Этап 3: Установка программ на сервер

Выполняйте команды по очереди, дожидаясь завершения каждой.

### Шаг 3.1: Установка Node.js 20.x

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs
```

Проверьте установку:

```bash
node -v
npm -v
```

Должно показать: `v20.x.x` и `10.x.x`

### Шаг 3.2: Установка Git

```bash
apt install -y git
git --version
```

### Шаг 3.3: Установка Redis (для очередей задач)

```bash
apt install -y redis-server
systemctl enable redis
systemctl start redis
systemctl status redis
```

Должно быть написано `active (running)`. Нажмите `Ctrl + C` для выхода.

### Шаг 3.4: Установка Nginx (веб-сервер)

```bash
apt install -y nginx
systemctl enable nginx
systemctl start nginx
systemctl status nginx
```

### Шаг 3.5: Установка PM2 (менеджер процессов)

```bash
npm install -g pm2
pm2 -v
```

### Шаг 3.6: Установка Certbot (для HTTPS)

```bash
apt install -y certbot python3-certbot-nginx
```

---

## Этап 4: Загрузка проекта с GitHub

### Шаг 4.1: Создание пользователя для проекта

```bash
adduser storyboard
```

Система спросит пароль — придумайте и введите его дважды.  
Остальные поля можно оставить пустыми (нажимать Enter).

### Шаг 4.2: Клонирование проекта

**Вариант A: Если проект публичный на GitHub**

```bash
su - storyboard
cd /home/storyboard
git clone https://github.com/ВАШ-НИК/storyboard-ai.git
cd storyboard-ai
```

**Вариант B: Если проект приватный**

1. На GitHub перейдите в настройки аккаунта → Developer settings → Personal access tokens
2. Создайте токен с правами `repo`
3. Клонируйте с токеном:

```bash
su - storyboard
cd /home/storyboard
git clone https://ВАШ-ТОКЕН@github.com/ВАШ-НИК/storyboard-ai.git
cd storyboard-ai
```

### Шаг 4.3: Установка зависимостей

```bash
# Установка зависимостей корневого проекта
npm install

# Установка зависимостей API
cd apps/api
npm install
npm run build
cd ../..

# Установка зависимостей Web (frontend)
cd apps/web
npm install
npm run build
cd ../..
```

Это может занять 10-20 минут.

### Шаг 4.4: Настройка Python workers

```bash
cd apps/workers

# Создание виртуального окружения Python
python3 -m venv venv

# Активация окружения
source venv/bin/activate

# Установка зависимостей Python
pip install -r requirements.txt
# ИЛИ если есть pyproject.toml:
pip install -r pyproject.toml

# Выход из окружения
deactivate

cd ../..
```

---

## Этап 5: Настройка проекта

### Шаг 5.1: Создание файла .env.local

```bash
cd /home/storyboard/storyboard-ai
nano .env.local
```

Откроется текстовый редактор.

### Шаг 5.2: Заполнение переменных

Скопируйте содержимое ниже и вставьте в редактор (Ctrl+Shift+V):

```env
# ===========================================
# SUPABASE (База данных + Авторизация)
# ===========================================
NEXT_PUBLIC_SUPABASE_URL=https://ВАШ-ПРОЕКТ.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-anon-ключ
SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-ключ

# ===========================================
# AI Сервисы (генерация изображений)
# ===========================================
REPLICATE_API_KEY=r8_ваш-ключ-replicate
OPENAI_API_KEY=sk-ваш-ключ-openai

# ===========================================
# STRIPE (Платежи)
# ===========================================
STRIPE_SECRET_KEY=sk_test_ваш-ключ
STRIPE_PUBLISHABLE_KEY=pk_test_ваш-ключ
STRIPE_WEBHOOK_SECRET=whsec_ваш-секрет

# ===========================================
# REDIS (Очереди задач)
# ===========================================
REDIS_URL=redis://localhost:6379

# ===========================================
# ПРИЛОЖЕНИЕ
# ===========================================
NODE_ENV=production
NEXT_PUBLIC_APP_URL=http://ВАШ-IP-ИЛИ-ДОМЕН
NEXT_PUBLIC_API_URL=http://ВАШ-IP-ИЛИ-ДОМЕН:4000

API_PORT=4000
API_URL=http://localhost:4000
WORKERS_URL=http://localhost:8000
JWT_SECRET=случайная-строка-минимум-64-символа

# ===========================================
# ЛИМИТЫ
# ===========================================
MAX_FILE_SIZE_MB=10
MAX_SCENES_PER_PROJECT=100
MAX_CONCURRENT_GENERATIONS=3

# ===========================================
# ЛОГИРОВАНИЕ
# ===========================================
DEBUG_AI=false
LOG_LEVEL=info
```

**Важные моменты:**

1. **JWT_SECRET** — сгенерируйте случайную строку:
   ```bash
   openssl rand -base64 48
   ```
   Скопируйте вывод и вставьте вместо `случайная-строка...`

2. **SUPABASE** — создайте проект на https://supabase.com, скопируйте URL и ключи из настроек

3. **REPLICATE** — получите ключ на https://replicate.com

4. **OPENAI** — получите ключ на https://platform.openai.com

### Шаг 5.3: Сохранение файла

1. Нажмите `Ctrl + O`
2. Нажмите `Enter` (подтвердить имя файла)
3. Нажмите `Ctrl + X` (выход)

---

## Этап 6: Запуск проекта

### Шаг 6.1: Запуск через PM2

```bash
# Запуск API
cd /home/storyboard/storyboard-ai/apps/api
pm2 start dist/index.js --name storyboard-api

# Запуск Web (frontend)
cd /home/storyboard/storyboard-ai/apps/web
pm2 start npm --name storyboard-web -- start

# Запуск AI Workers
cd /home/storyboard/storyboard-ai/apps/workers
source venv/bin/activate
pm2 start -n storyboard-workers --interpreter python3.11 src/main.py
deactivate

# Проверка статуса
pm2 status
```

Все сервисы должны быть в статусе `online`.

### Шаг 6.2: Сохранение конфигурации PM2

```bash
# Сохранить список процессов для автозапуска
pm2 save

# Настроить автозапуск при загрузке сервера
pm2 startup
```

Последняя команда покажет команду для копирования — выполните её!

### Шаг 6.3: Проверка локально

```bash
# Проверка API
curl http://localhost:4000/health

# Проверка Web
curl http://localhost:3000
```

---

## Этап 7: Настройка домена и HTTPS

### Вариант A: Без домена (тестовый режим)

Пропустите этот этап. Доступ будет по IP: `http://123.45.67.89:3000`

### Вариант B: С доменом (рекомендуется)

#### Шаг 7.1: Покупка домена

1. В панели Beget перейдите в раздел **«Домены»**
2. Нажмите **«Зарегистрировать домен»**
3. Введите желаемое имя (например: `storyboard-ai.ru`)
4. Оплатите (от 199 руб/год)

#### Шаг 7.2: Привязка домена к серверу

1. В панели Beget → Домены найдите ваш домен
2. Нажмите **«Управление DNS»**
3. Добавьте A-запись:

```
Тип: A
Имя: @
Значение: 123.45.67.89 (ваш IP)
TTL: 3600
```

4. Для поддомена API добавьте ещё одну запись:

```
Тип: A
Имя: api
Значение: 123.45.67.89
TTL: 3600
```

**Подождите 15-60 минут** для обновления DNS.

#### Шаг 7.3: Настройка Nginx

```bash
# Конфигурация для Web
sudo nano /etc/nginx/sites-available/storyboard
```

Вставьте (замените `ваш-домен.ru` на ваш домен):

```nginx
server {
    listen 80;
    server_name ваш-домен.ru www.ваш-домен.ru;

    client_max_body_size 10M;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    location /_next/static {
        proxy_pass http://localhost:3000;
        proxy_cache_valid 60m;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }
}
```

Сохраните: `Ctrl+O` → `Enter` → `Ctrl+X`

```bash
# Конфигурация для API
sudo nano /etc/nginx/sites-available/api
```

Вставьте:

```nginx
server {
    listen 80;
    server_name api.ваш-домен.ru;

    location / {
        proxy_pass http://localhost:4000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

Сохраните: `Ctrl+O` → `Enter` → `Ctrl+X`

```bash
# Включение конфигураций
sudo ln -s /etc/nginx/sites-available/storyboard /etc/nginx/sites-enabled/
sudo ln -s /etc/nginx/sites-available/api /etc/nginx/sites-enabled/

# Удаление стандартной конфигурации
sudo rm /etc/nginx/sites-enabled/default

# Проверка конфигурации
sudo nginx -t

# Перезапуск Nginx
sudo systemctl restart nginx
```

#### Шаг 7.4: Получение SSL сертификата (HTTPS)

```bash
# Для основного домена
sudo certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru

# Для API поддомена
sudo certbot --nginx -d api.ваш-домен.ru
```

Certbot автоматически:
- Получит бесплатный SSL сертификат от Let's Encrypt
- Настроит HTTPS
- Настроит автообновление

**Готово!** Теперь сайт доступен по HTTPS.

---

## Этап 8: Проверка работы

### Шаг 8.1: Проверка сервисов

```bash
pm2 status
```

Все сервисы должны быть `online`.

### Шаг 8.2: Проверка в браузере

Откройте браузер и перейдите:

- **С доменом:** https://ваш-домен.ru
- **Без домена:** http://123.45.67.89:3000

### Шаг 8.3: Проверка API

```bash
curl https://api.ваш-домен.ru/health
```

Должно вернуть JSON с информацией о статусе.

---

## 🔧 Управление проектом

### Просмотр логов

```bash
# Логи всех сервисов
pm2 logs

# Логи конкретного сервиса
pm2 logs storyboard-api
pm2 logs storyboard-web
pm2 logs storyboard-workers

# Выход из логов: Ctrl + C
```

### Перезапуск сервисов

```bash
# Перезапустить всё
pm2 restart all

# Перезапустить конкретный сервис
pm2 restart storyboard-api
```

### Остановка проекта

```bash
pm2 stop all
```

### Обновление проекта

```bash
cd /home/storyboard/storyboard-ai
git pull
npm run build
pm2 restart all
```

---

## ⚠️ Частые проблемы

### Проблема 1: Сервис не запускается

```bash
# Посмотрите логи
pm2 logs storyboard-api --lines 50

# Проверьте .env.local
cat /home/storyboard/storyboard-ai/.env.local
```

### Проблема 2: Ошибка "Cannot find module"

```bash
cd /home/storyboard/storyboard-ai/apps/api
rm -rf node_modules
npm install
npm run build
pm2 restart storyboard-api
```

### Проблема 3: Ошибка Redis

```bash
# Проверьте статус
systemctl status redis

# Перезапустите
systemctl restart redis
```

### Проблема 4: Nginx возвращает 502 Bad Gateway

```bash
# Проверьте, что сервисы запущены
pm2 status

# Проверьте логи Nginx
sudo tail -f /var/log/nginx/error.log
```

### Проблема 5: Не открывается сайт по домену

1. Подождите 15-60 минут (обновление DNS)
2. Проверьте DNS: https://dnschecker.org
3. Проверьте Nginx: `sudo nginx -t`

---

## 💰 Итоговая стоимость

| Услуга | Стоимость |
|--------|-----------|
| VPS Beget (2 ядра, 2 GB) | 450 руб/мес |
| Домен .RU | 199 руб/год |
| Supabase (бесплатный тариф) | 0 руб |
| **Итого в месяц** | **~470 руб** |

---

## 📞 Полезные ссылки

- **Панель Beget:** https://beget.com
- **Поддержка Beget:** https://beget.com/support
- **Документация PM2:** https://pm2.keymetrics.io
- **Supabase:** https://supabase.com
- **Replicate AI:** https://replicate.com

---

## ✅ Чек-лист завершения

Пройдитесь по списку:

- [ ] VPS заказан и активирован
- [ ] Подключение по SSH работает
- [ ] Node.js установлен (`node -v`)
- [ ] Redis установлен и запущен
- [ ] Nginx установлен
- [ ] PM2 установлен
- [ ] Проект склонирован с GitHub
- [ ] Зависимости установлены
- [ ] Файл `.env.local` создан и заполнен
- [ ] Сервисы запущены через PM2
- [ ] `pm2 save` выполнен
- [ ] `pm2 startup` выполнен
- [ ] Сайт открывается в браузере
- [ ] HTTPS настроен (если есть домен)

---

## 🎉 Поздравляем!

Ваш проект Storyboard AI теперь работает в интернете!

**Следующие шаги:**
1. Зарегистрируйтесь на сайте через форму регистрации
2. Настройте Supabase (выполните миграцию БД)
3. Протестируйте генерацию раскадровок
4. Настройте платежи через Stripe (опционально)

---

*Инструкция создана: 23 февраля 2026*  
*Для вопросов и поддержки обратитесь к документации проекта*
